import { AlphabetCheckDirective } from './alphabet-check.directive';

describe('AlphabetCheckDirective', () => {
  it('should create an instance', () => {
    const directive = new AlphabetCheckDirective();
    expect(directive).toBeTruthy();
  });
});
